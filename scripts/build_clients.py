"""Build the clients.xlsx template with sample rows."""
from openpyll import Workbook
from openpyll.styles import Font, PatternFill, Alignment, Border
from pathlib import Path

from common import ROOT
